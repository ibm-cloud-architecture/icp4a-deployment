apiVersion: securityenforcement.admission.cloud.ibm.com/v1beta1
kind: ImagePolicy
metadata:
  name: image-policy
  namespace: "{{ namespace }}"
spec:
  repositories:
  - name: docker.io/*
    policy: null
  - name: "{{ register_server }}/*"
    policy: null